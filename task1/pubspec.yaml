name: task1
description: A task management app for university students.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^5.2.1
  googleapis: ^10.0.0
  googleapis_auth: ^1.4.1
  http: ^0.13.4
  provider: ^6.0.3
  intl: ^0.17.0
  uuid: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true